name: myapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0-278.1.beta

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9       # ✅ Add camera plugin
  image_picker: ^1.1.0    # ✅ Used to pick image from gallery/camera
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.8
  logging: ^1.3.0
  go_router: ^16.1.0
  permission_handler: ^12.0.1 # ✅ Handle runtime permissions

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Optional: If you want to add assets like icons/images in the future
  # assets:
  #   - assets/images/
